
spring:
  profiles: test
  data:
    mongodb:
      host: localhost

name: POS-Backend-Test

efaps:
  restUrl: http://localhost:8888/servlet/rest
  productPath: /products

sso:
  url: http://my.sso.com/pe
  postValues:
    client_id: ${random.value}
    username: ${random.value}
    password: ${random.value}
    grant_type: password
    client_secret: ${random.value}
